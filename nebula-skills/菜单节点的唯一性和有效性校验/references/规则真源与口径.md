# 规则真源与口径

## 唯一真源

- 文档：`nebula/docs/plans/前端的表单校验规则.md`
- **禁止**用本地未上传的 `seccenter` 实现覆盖文档（例如旧版「perm 全表唯一」）。

## 唯一性与耦合（文档摘要）

| 对象 | 规则 |
|------|------|
| directory / page `route_path` | 同 `project_id` 下，`type!=function` 的非空 path 唯一；不支持 `*` |
| function `route_path` | 同项目下不得与**父链**上任一节点 path 全等；任意段可 `*` |
| page `route_path` + `params` | **全库**（type=page）唯一 |
| 非 page | `params` 必须为空 |
| function `perm` | 必填；**同 `parent_id`** 下唯一（允许跨父复用） |
| 非 function | `perm` 必须为空 |

## 扫描实现（本 skill）

| 文件 | 作用 |
|------|------|
| `scripts/convert-menu-yaml-to-json.py` | YAML → MenuVO JSON；文件名作 `projectId` |
| `scripts/scan-menu-rules.mjs` | CLI |
| `scripts/lib/scan-menu-rules.mjs` | 扫描核 |
| `scripts/lib/menu-path-rules.mjs` | `chkPathDup` / `chkAncPath` |
| `scripts/lib/path-syntax.mjs` | 轻量 path 语法 |

## 表单产品代码（apex_dev，可选改动）

| 能力 | 路径 |
|------|------|
| 表单挂载 | `src/views/system/menu/components/MenuFormDialog.vue` |
| 路径语法工厂 | `src/utils/formRules.ts` |

**禁止**在 apex 放置 `scan-menu-rules` 或 `convert-menu-yaml-to-json`。
