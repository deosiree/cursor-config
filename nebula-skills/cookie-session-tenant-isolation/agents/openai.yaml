interface:
  display_name: "Cookie租户隔离"
  short_description: "解释并排查 microfb 的 Cookie-Session 租户隔离链路，明确前后端职责边界"
  default_prompt: "使用 $cookie-session-tenant-isolation 审核当前链路：登录、Set-Cookie、withCredentials、verifySession 与租户隔离是否一致。"
