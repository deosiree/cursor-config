# Darwin 评估：api-gateway-add（重构后 · 通用性专评）

- **评估日**：2026-05-20
- **范围**：write-skill 重构后（通则一/二上提、租户样本降级为印证）
- **模式**：结构 1–7 主 agent；维度 8 六条 test-prompt **dry_run**（独立子 agent 干跑）
- **对比基线**：round3 **91.8**（重构前，租户编排叙事仍偏重）

## 总览

| 指标 | round3（重构前） | 本次（重构后） |
|------|------------------|----------------|
| **Darwin 总分** | 91.8 | **92.4** |
| **通用性专评** | 约 72（推断） | **86 / 100** |
| **eval_mode** | mixed | dry_run（#1–#6 全量干跑） |

重构后总分略升；**通用性提升显著**（+14），主要来自叙事从「编排专用」改为「两条跨模块通则 + A/B/C 分型」。

---

## 8 维评分（重构后）

| 维度 | 权重 | 分(1–10) | 加权 | 观察 |
|------|------|----------|------|------|
| Frontmatter 质量 | 8 | **10** | 8.0 | description 明示通则、任意模块、触发词完整 |
| 工作流清晰度 | 15 | **9** | 13.5 | 先 A/B/C 再 RED/GREEN；主 SKILL 84 行未膨胀 |
| 边界条件覆盖 | 10 | **8** | 8.0 | 部分成功态仍在 orchestration；**主 SKILL 未写 deprecate 边界** |
| 检查点设计 | 7 | 8 | 5.6 | 编排/跨域「确认后再改码」在 feature-skill |
| 指令具体性 | 15 | **9** | 13.5 | `gateway-atomic-vs-integration` 三种合法形态 + 反例 |
| 资源整合度 | 5 | 9 | 4.5 | 新 references + 重命名 feature-skill；链接已对齐 |
| 整体架构 | 15 | **10** | 15.0 | **通则 → references → feature → template 印证**，层次正确 |
| 实测表现 | 25 | **9** | 22.5 | 6/6 dry_run pass；#4 未误套租户逻辑 |
| **合计** | 100 | | **92.4** | ≥90 达标 |

---

## 通用性专评（100 分制）

| 子项 | 权重 | 分 | 说明 |
|------|------|-----|------|
| 通则抽象度 | 25 | **23** | 通则一/二独立于业务域；`gateway-atomic-vs-integration` 开篇即声明跨模块 |
| 非租户场景可路由 | 25 | **22** | 主 SKILL 使用示例 + `gateway-patterns-green.md` 覆盖菜单多项目导出；#4 test 明确 |
| 样本误用防护 | 20 | **17** | template/README、SKILL L25 声明租户仅为印证；**通则 doc 正例仍以租户为先**（可读性代价） |
| 测试集平衡 | 15 | **14** | 6 条：A×1、B×2、C×1、边界×1、混调×1；缺「纯类型 A + 通则二」组合 |
| 与四层接入共存 | 15 | **10** | 双轨清晰；agent 仍可能先读四层再忽略通则（依赖 RED 第 2 步 A/B/C） |
| **合计** | 100 | **86** | **良好**；较重构前约 +14 |

### 通用性结论

- **已解决**：「租户删除 = 本 skill 全部能力」的误导；类型 B/C 与菜单导出有独立路径。
- **仍弱**：
  1. 主 `SKILL.md` 未写「旧接口下线 → `api-gateway-deprecate`」（#5 靠 agent 常识，README 有提）。
  2. 通则文档 §3 正例顺序：租户 → 单原子 → 循环导出；建议把 **3.2/3.3 提前** 或增加「非租户优先」提示（P3 可读性，非 P0）。
  3. `gateway-patterns-green` 未进主 SKILL「子能力路由」表，仅在使用示例与 few-shot 索引。

---

## test-prompt 干跑（维度 8）

| id | 场景 | pass | 通用性 | vs baseline |
|----|------|------|--------|-------------|
| 1 | 菜单 3 写接口 | ✅ | — | better |
| 2 | 租户解绑+删除 | ✅ | ✅ 样本≠专用 | better |
| 3 | 环依赖 | ✅ | ✅ template 仅示例 | better |
| **4** | **多项目菜单导出** | ✅ | **✅ 未套 tenant 逻辑** | better |
| 5 | 删旧 API | ✅ | — | better（主文档边界略弱） |
| 6 | 集成内混调 API | ✅ | ✅ 通则 §4 | better |

**#4 判定标准**：若输出出现 `unbindAll` / `TenantAPI.delete` / 解绑设备序列 → `generality_ok: false`。

---

## 与 round3 对比

| 维度 | Δ | 说明 |
|------|-----|------|
| 整体架构 | **+1.5** | 通则上提、样本降级 |
| Frontmatter | **+0.8** | 跨模块触发词 |
| 边界条件 | **-1.0** | deprecate 未入主 SKILL |
| 实测表现 | **-2.5** | 本次全 dry_run；round3 有 #2/#6 full_test |
| 通用性（专评） | **~+14** | 用户关切项，重构主收益 |

---

## 建议（evaluate-only，未改文件）

| 优先级 | 动作 | 预期收益 |
|--------|------|----------|
| P1 | 主 SKILL「子能力路由」+1 行：`api-gateway-deprecate` 边界 | 边界 +1、#5 稳定 pass |
| P2 | 主 SKILL 路由表链到 `gateway-patterns-green.md` | 通用性 +2、#4 更稳 |
| P3 | `gateway-atomic-vs-integration` §3 增加「非租户优先阅读 3.2–3.3」 | 可读性，减样本锚定 |
| 可选 | #1/#4 子 agent full_test | 维度 8 可冲 94+ |

---

## P1 优化（已执行 · keep）

- **改动**：`SKILL.md` 子能力路由 +1 行 → 旧接口下线引导 `api-gateway-deprecate`，不展开 GREEN
- **commit**：`p1-deprecate-boundary`
- **复评**：边界条件 8→9；总分 **92.4 → 93.4**（dry_run，#5 稳定指向 deprecate）
