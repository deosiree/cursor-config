# feature-skills

两条**跨模块** gateway 通则的功能入口（细则在 `references/`）：

| 目录 | 通则 |
|------|------|
| `[[网关原子与集成错误分层/SKILL.md]]` | 通则一：原子 vs 集成、`handleGatewayError` |
| `[[跨Gateway动态引用/SKILL.md]]` | 通则二：动态 import |

历史目录 `网关编排与原子集成错误分层` 已合并重命名为 `网关原子与集成错误分层`。
