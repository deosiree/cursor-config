# writing-skills core 摘要

本 skill 套件遵循 `$写skill` 的固定交付约束：

- `README.md` 给维护者看
- `SKILL.md` 给 agent 看
- `template/` 放人类可仿写样例
- `assets/` 放 agent 轻量素材
- `references/` 放长说明
- `evals/` 放触发与输出验收样例

额外固定要求：

- `SKILL.md` frontmatter 使用中文
- 主 `SKILL.md` 保留 `RED / GREEN / REFACTOR`
- 模板不能退化成对单一仓库文档的硬拷贝
