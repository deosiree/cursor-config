# opsdeck-453b4aa

## 来源

- 标识：`opsdeck-453b4aa`
- 适用变体：`新i18n-纯ts中用i18n.global.t` 在 `opsdeck` 中的实现变体。

## 模板类型

- `before/after`

## 与主模板的关系

- 这是 `新i18n-纯ts中用i18n.global.t` 的独立成功历史版本示例。
- 主模板用于快速理解最核心闭环；当前 few-shot 用于覆盖纯 TS 全局实例消费这一类实现。
- 当当前仓库和主模板接近时，可直接复用这个 few-shot。
