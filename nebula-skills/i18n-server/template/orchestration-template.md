# 编排-i18n迁移模板

## 目标仓库

- 仓库：
- 当前国际化状态：
- 目标方案：
- 是否已有链路分析：

## 分析前置

- `chainAnalysisAvailable`：
- `chainAnalysisSource`：
- `analysisConfidence`：
- 若不足，先进入 `[[intention-skills/分析-i18n链路]]`

## 候选方案

1. 方案 A：
   - `intentionPath`：
   - `featureSkillSequence`：
   - `changeSurfaceSize`：
   - `beforeChain`：
   - `afterChain`：
   - `analysisBasis`：
2. 方案 B：
   - `intentionPath`：
   - `featureSkillSequence`：
   - `changeSurfaceSize`：
   - `beforeChain`：
   - `afterChain`：
   - `analysisBasis`：

## 推荐结果

- `recommendedPlan`：
- `whyRecommended`：
- `rejectedPlans`：

## 验证

- 构建 / 类型检查
- 语言切换
- qiankun 同步
- 规则文案是否随语言切换重新计算
