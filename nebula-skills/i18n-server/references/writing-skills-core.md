# i18n-server 套件化原则

本套件按 `写skill` 的固定交付结构整理：

- 顶层父 skill 负责任务会话路由
- `intention-skills/` 承载分析、策略、编排和功能路由
- `feature-skills/` 承载源码级功能能力
- `template/` 与 `assets/` 分层：前者给人类评审与仿写，后者给 agent 复用

## 维护原则

- 解释性文档不能替代真实源码模板
- 新经验优先回填到最接近的既有 skill，而不是新建平行案例
- `分析-i18n链路` 是横向公共前置能力，优先复用而不是重复猜链路
