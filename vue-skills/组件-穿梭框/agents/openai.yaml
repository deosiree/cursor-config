interface:
  display_name: "组件-穿梭框"
  short_description: "Transfer v1 + DeviceTransfer v2；默认 v2 页面接入；先判定新增或更新"
  default_prompt: "使用 $组件-穿梭框：先判定新增/更新。无 transfer 或缺 DeviceTransfer → 新增套件（mvp + v2-mvp）。改页面默认 更新-页面接入Transfer_v2 对照 template/v2-after；仅人类要求 v1 时用 更新-页面接入Transfer 与 template/before|after。"
