interface:
  display_name: "表单校验-规则工厂formRules"
  short_description: "formRules、表单校验、routePath/apiUrl pathLike、pwdPair、密码策略、trimFieldOnBlur、标识符命名、ruleStyle、盘点下一项；可先只读推荐再实施；不含 i18n。"
  default_prompt: "使用 $表单校验-规则工厂formRules：若 apex/microfb formRules 变了要更新 skill 样本，走维护-从业务仓同步样本（dry-run 再 apply，真源 apex）。若仅 repoRoot/moduleHint 且要推荐下一表单项，先走盘点（只读）。若已有 componentPath+fields，则 Step1-2 → 阶段A 配置工厂 → 阶段B 接入。密码走 pwdPair。未命中 style 则 MVP+Plan。改 skill 样本后 sync-samples。不要改 locale。"
