interface:
  display_name: "组件-操作列折叠"
  short_description: "操作列槽位折叠(calcOpStrip)+OpItem；inline-visible-count为槽位总数；列宽离屏探针"
  default_prompt: "使用 $组件-操作列折叠：先判定新增/更新；template/mvp 含 calcOpStrip；槽位语义见 references/slot-semantics.md；业务只传 list-data-length。"
