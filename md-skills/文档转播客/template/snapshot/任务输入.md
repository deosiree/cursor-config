# 任务输入（Snapshot 对标本例）

```text
把这篇面经 md 转成搭档播客（未指定模式 → 默认完整，不二次确认）。
源文：阿里面试官冷笑：现在上下文窗口都 200 万 token 了，你的 RAG 还有存在的必要吗？ 我算了一笔账，他沉默了.md
输出目录：与源文 stem 同名子目录（例 …/阿里面试官冷笑…/）
```

**判定结果**：`doc_type: 面经`，`N=5`，`K=5`，`delivery_mode: 完整`（默认，无需询问）。
