# 检查点 A（test#4 · with_skill）

源文约 **10** 个章节（N=10），单集朗读稿会过长。请二选一：

1. **合并**：把相近章节并成 ≤7 个主题块，出一集；
2. **拆集**：按章节拆成多集（例如上/下），每集 N≤7。

确认后我再写 `播客朗读稿-[主题].md` 并走 validate / TTS 流程。

（本回复在用户确认前**不**生成 10 章全长朗读稿。）
