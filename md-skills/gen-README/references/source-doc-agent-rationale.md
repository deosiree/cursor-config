# 为什么这是 Agent 而不是单轮编排器

这个 skill 的核心不是“把子节点按固定顺序跑完”，而是：

1. 观察仓库与现有文档。
2. 判断认知是否足够。
3. 必要时向人提问。
4. 依据新认知继续执行。
5. 允许多次循环直到收敛。

因此它更接近一个可多轮迭代的 agent：

- 会暂停
- 会提问
- 会根据回答改变后续路径
- 会在验证失败后回到前一轮继续收敛

`template/microfb/` 在这个模型里只是 few-shot 成品案例，不是默认模板源。

