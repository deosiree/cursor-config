# writing-skills 核心落点

本 skill 继承的核心约束：

- 先看失败基线，再写规则。
- 主 `SKILL.md` 只保留激活必需规则。
- 长说明下沉到 `README.md` 与 `references/`。
- 通过 `RED -> GREEN -> REFACTOR` 收紧触发与输出稳定性。

对于本 skill，最重要的失败基线是：

- 不扫仓库直接写文档
- 不确定时硬猜
- 只产正文，不维护索引
- 把历史案例误当默认输入

