# 真实输出

## 待核对规则

1. 图表类型必须先判定，再进入 Mermaid 质检
2. Mermaid 修复只能修语法与可读性，不得擅自改图意
3. 子 skill 模板必须来自真实试跑案例，而不是空骨架

## 本地依据摘录

- 父级 `SKILL.md`
  - “需要出图时，必须先做图类型判断，再做图语法质检。”
- `docs/Mermaid.md`
  - “所有节点文本都用双引号包裹”
  - “节点 ID 使用短英文”
- `docs/sequenceDiagram.md`
  - “alt / else 是互斥关系，不是并发关系”
  - “回跳与重定向统一用返回箭头 `-->>` 表达结果态”

## 外部依据占位

- Mermaid 官方 flowchart 语法
- Mermaid 官方 sequenceDiagram 语法
- Cursor Skills / Agent Skills 关于 few-shot 与模板分层的约束

## 回写建议

- 保留父级“图类型先判定”的硬门禁
- 在 `Mermaid图语法修复` 节点中继续强调“修语法不改图意”
- 在父级清单中保留“子 skill 模板必须是真实试跑产物”

## 不确定项

- 是否需要把“外部依据占位”升级成固定引用格式
- 是否需要额外为 `sequenceDiagram` 补单独的 few-shot 核对记录
