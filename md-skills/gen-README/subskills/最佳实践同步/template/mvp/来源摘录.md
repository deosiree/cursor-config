# 来源摘录

来源文件：

- `SKILL.md`
- `docs/Mermaid.md`
- `docs/sequenceDiagram.md`

原文摘录：

> 需要出图时，必须先做图类型判断，再做图语法质检。

> 所有节点文本都用双引号包裹：`A["text"]`

> 节点 ID 使用短英文（如 `LOADCFG`、`WRITE`），不要使用长中文句子作为 ID。

> alt / else 是互斥关系，不是并发关系。

> 回跳与重定向统一用返回箭头 `-->>` 表达结果态。

这些摘录共同构成“本地最佳实践”的核对基线。
