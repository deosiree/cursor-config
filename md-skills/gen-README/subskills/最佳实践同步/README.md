# 最佳实践同步

本 skill 采用“本地中文模式” frontmatter，用于给当前规则提供外部依据。

本节点按“新增型 skill”维护，真实模板位于 `template/mvp/` 与 `template/snapshot/`，内容直接拆自父级当前规则与 `microfb` 真实案例。

- 上游：父级当前争议点、本地 skill 规则
- 下游：父级规则回写、子节点约束更新
- 必须回退人工澄清：无法明确要验证的规则范围时

```text
请先确认要验证的规则范围，再同步外部最佳实践。
```
