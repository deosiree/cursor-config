# 真实输出

| 场景 | 文档名 | 输入特征摘录 | 图类型结论 | 判定理由 | 歧义时回退 |
| --- | --- | --- | --- | --- | --- |
| 系统边界拓扑 | `前端架构拓扑图.md` | “输出可版本化的静态架构拓扑图，用于团队对齐边界、依赖方向与关键链路入口” | `flowchart TD` | 该场景强调系统边界、组件关系、依赖方向，而不是多参与者交互顺序 | 若同一文档新增大量跨参与者时序，再拆出独立 `sequenceDiagram` |
| 登录到首屏运行时 | `运行时拓扑_登录到首屏.md` | “从进入页面到可用首屏的关键交互讲清楚，并与源码入口一一对应” | `sequenceDiagram` | 该场景核心是 `User -> Router -> Guard -> PermissionStore -> registerApps` 的调用顺序与分支 | 若后续只保留静态参与者关系，可单独补 `flowchart TD` 概览 |

真实判定记录片段：

```text
场景 A：
文档目标是“团队对齐边界、依赖方向与关键链路入口”
=> 判定为 flowchart TD

场景 B：
文档目标是“把从进入页面到可用首屏的关键交互讲清楚”
=> 判定为 sequenceDiagram
```
