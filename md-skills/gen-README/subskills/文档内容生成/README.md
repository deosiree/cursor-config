# 文档内容生成

本 skill 采用“本地中文模式” frontmatter，用于把上游已收敛的事实真正写成文档。

本节点按“新增型 skill”维护，真实模板位于 `template/mvp/` 与 `template/snapshot/`，内容直接拆自父级 `template/microfb/`。

- 上游：文档结构规划、源码证据抽取、源码符号定位、图类型判定
- 下游：Mermaid图语法修复、README索引维护
- 必须回退人工澄清：关键章节缺少事实支撑时

```text
请根据既定结构和证据生成正文；若关键事实缺失，先指出问题并提问。
```
