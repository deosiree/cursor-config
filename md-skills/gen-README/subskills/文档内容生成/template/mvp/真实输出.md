# 真实输出

正文片段示例：

`microfb` 的菜单刷新链路以缓存菜单为主输入。路由守卫进入入口路径后，会优先读取 `readMenuCache()` 结果；若命中首个菜单路径，则直接跳转，否则再进入动态路由生成流程。对应的菜单生成规则在《菜单模型与动态路由生成说明》中定义，关键缓存读写点见《流程图符号定位表》中的 `readMenuCache()` / `writeMenuCache()`。

正文结构样板：

```text
目标
关键链路
源码落点
异常与回退
```
