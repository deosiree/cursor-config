# 源码符号定位

本 skill 采用“本地中文模式” frontmatter，用于把文档节点落实到源码证据。

本节点按“新增型 skill”维护，真实模板位于 `template/mvp/` 与 `template/snapshot/`，内容直接拆自父级 `template/microfb/`。

- 上游：架构拓扑映射、源码证据抽取、文档草稿
- 下游：文档内容生成、图表说明
- 必须回退人工澄清：多个候选实现无法裁决时

```text
请把当前文档中的关键节点映射到源码文件和读写点。
```
