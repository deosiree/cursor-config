# README索引维护

本 skill 采用“本地中文模式” frontmatter，用于维护多文件文档的导航入口。

本节点按“新增型 skill”维护，真实模板位于 `template/mvp/` 与 `template/snapshot/`，内容直接拆自父级 `template/microfb/README.md`。

- 上游：文档结构规划、文档内容生成、最终产物清单
- 下游：最终交付
- 必须回退人工澄清：导航层级或目标读者路径不清楚时

```text
请根据最新产物清单更新 README 索引，并指出缺失链接。
```
