# 源码证据抽取

本 skill 采用“本地中文模式” frontmatter，用于在写作前先沉淀事实材料。

本节点按“新增型 skill”维护，真实模板位于 `template/mvp/` 与 `template/snapshot/`，内容直接拆自父级 `template/microfb/`。

- 上游：仓库扫描、架构拓扑映射、现有 docs
- 下游：源码符号定位、文档内容生成、文档结构规划
- 必须回退人工澄清：关键事实在仓库内无法确认时

```text
请先抽取与目标主题相关的源码事实、冲突点和缺失信息。
```
