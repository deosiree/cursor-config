# 真实输出

- 事实 1：`microfb` 是 qiankun 主应用，`registerApps()` 在 `router.isReady()` 后启动
- 事实 2：动态路由生成以 `readMenuCache().menus` 为 v2 优先输入，v1 为回退
- 事实 3：`finalizeV2Login()` 负责写入 `userInfo`、菜单缓存、同步 props、跳转首个菜单
- 冲突点：若现有文档把菜单生成写成“只走 v1 接口”，需要标记为过时
- 缺失信息：若当前任务要覆盖某个未在 `microfb` 案例中出现的新模块，需回到人工澄清

来源片段样板：

```text
运行时链路：
User -> Router -> Guard -> PermissionStore -> registerApps

菜单策略：
v2 优先、v1 回退
```
