# 文档结构规划

本 skill 采用“本地中文模式” frontmatter，用于定义文档矩阵、目录层级和索引关系。

本节点按“新增型 skill”维护，真实模板位于 `template/mvp/` 与 `template/snapshot/`，内容直接拆自父级 `template/microfb/`。

- 上游：架构拓扑映射、源码证据抽取、父级目标约束
- 下游：文档内容生成、README索引维护
- 必须回退人工澄清：拆分维度或受众边界不清楚时

```text
请根据仓库结构和目标受众，把文档拆成最小可维护的多文件结构。
```
