# 架构拓扑映射

本 skill 采用“本地中文模式” frontmatter，用于在父级 agent 的前期阶段建立架构与运行时骨架。

本节点按“新增型 skill”维护，真实模板位于 `template/mvp/` 与 `template/snapshot/`，内容直接拆自父级 `template/microfb/`。

- 上游：仓库扫描结果、父级当前认知
- 下游：源码符号定位、文档结构规划、文档内容生成
- 必须回退人工澄清：入口、边界、依赖方向不清楚时

```text
基于仓库现状先给出系统边界和主链路，不要直接写完整正文。
```
