# 来源摘录（虚构短源文，用于 MVP）

```markdown
## useEffect 做什么

在函数组件里执行副作用：请求、订阅、手动改 DOM。

## 依赖数组

- 不写 []：每次渲染后都执行
- 写 []：只在挂载时执行一次
- 写 [a, b]：a 或 b 变化时执行

常见坑：依赖漏写导致闭包拿到旧 state，或依赖写错导致无限循环。
```

**统计**：N=2（两个二级标题为知识点）。
