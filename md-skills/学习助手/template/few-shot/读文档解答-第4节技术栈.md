# 读文档-解答 — 真实输出

**触发：** "回答第 4 节技术栈按层理解"  
**文档：** `Agent开发学习路线-抖音 AAA长安重卡胡师傅-国道钢卷转运.md`  
**方法论使用：** 苏格拉底追问 ×3 + 费曼解释 ×3 + 金字塔展开 ×2  
**双链引用：** 15+ 处跨第 1/2/3 节

---

## 产出摘要

### 苏格拉底追问段（开篇）

**追问 1：** 如果所有代码写在一个文件里，会怎样？
→ Agent 系统需要同时处理输入、推理、工具、上下文、状态——全混在一起，改一处炸全局。

**追问 2：** 为什么恰好是 7 层？
→ 不是设计出来的，是 craft-agents-oss 代码自然收敛的——每层有独立 package.json 和测试。

**追问 3：** 哪层最容易变垃圾桶？
→ Agent 后端层。权限应在 PermissionManager，裁剪应在 Context 层，路由应在 MCP Pool。

### 费曼解释段（逐层）

- **运行时层：** "Bun 就是 Agent 系统的操作系统"
- **UI 层：** "UI 是 Agent 的脸——四个技术分工：React 骨架、Tailwind 皮肤、Jotai 记忆、Radix 交互"
- **MCP 层：** "MCP 是 Agent 世界的 USB 接口——不管什么工具，实现 MCP Server 就能插进来"

### 金字塔总结段（收尾）

```
塔尖：每层只依赖下面。改 UI 不影响 Agent Loop，换模型不影响 MCP 工具池。
  ├── Day 1-2：运行时 + 传输层（地基）
  ├── Day 3-4：Agent 后端 + 工具协议（核心）
  ├── Day 5-6：UI + 桌面端（外壳）
  └── Day 7：校验与观测（保险网）
```

---

📂 **完整输出：** 见 `Agent开发学习路线-抖音 AAA长安重卡胡师傅-国道钢卷转运.md` 第 4 节（525-672 行）
