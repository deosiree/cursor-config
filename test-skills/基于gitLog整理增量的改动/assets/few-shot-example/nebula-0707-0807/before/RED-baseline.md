# RED 基线：问题根过宽（域名级）

## 现象

`build_problem_tree` 按 `cluster_key` 的**域名标签前缀**合并为 mega，问题根标题等于「租户管理」「菜单管理」「登录鉴权」等整域名称；真正有语义的「租户-创建流程」「权限-查写二分」被压成子问题。

## 数字（旧版）

| 项 | 值 |
| --- | --- |
| 问题根 | 14（≈ 域名标签数） |
| 子问题 | 53 |
| 提交 | 105 |

## 用户反馈

「问题太宽了，宽成域名标签了」

## after 目标

见 `after/acceptance.md`：29 主题问题根、4 子问题、域名只做列。
