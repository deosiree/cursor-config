# 管理中心首页用例 — 质量报告（0610）

> **历史报告（0610 导入前格式）**：表中「用例结果」列填写指当时格式。当前质检以 [`references/csv-export-format-rules.md`](../references/csv-export-format-rules.md) 为准。

## 交付物

| 文件 | 说明 |
|------|------|
| `docs/问题单/0610/dashboard.csv` | 19 条，UTF-8 BOM，可导入测试系统 |
| `configs/dashboard.cases.json` | 结构化源数据，含 `featureSet` 与 `expected` |

## 固定字段

- 子系统：`17`（来自 dashboard.csv 模板）
- 模块名：`管理中心首页界面`
- 创建人员：`惠岩`
- 路由：`/Apex/dashboard`
- 权限：`sys:dashboard:view`

## 功能集合分布（共 19 条）

| 功能集合 | 条数 | 类型 |
|----------|------|------|
| 页面加载 | 2 | 模板已有 |
| 页面权限 | 2 | 模板已有 |
| 数据概览 | 5 | **需测试工具手动添加** |
| 图表展示 | 4 | **需测试工具手动添加** |
| 筛选查询 | 2 | 模板已有 |
| 界面布局 | 2 | 模板已有 |
| 异常处理 | 2 | 模板已有 |

## 质量自检（ui-v2）

| 检查项 | 结果 |
|--------|------|
| 总用例数 | 19 |
| 用例ID 留空（导入分配） | 通过 |
| 全部用例已填「用例结果」 | 通过（19/19） |
| 功能集合均已填写 | 通过 |
| 单用例单验证点 | 通过 |
| 步骤用户可感知（无 F12/mock 于常规用例） | 通过 |
| 异常处理用例含模拟失败前置 | 通过（K2 例外） |

### 警告（可接受）

- 千分符、产品筛选差异等用例依赖环境数据量
- 资源使用情况卡片为置灰态，用例仅验 UI 不可交互，不断言业务数值
- 异常用例需测试环境可模拟接口失败

## 需在测试工具手动添加的功能集合

1. **数据概览**
2. **图表展示**

## G2 样例

**样例 1 — 页面加载**

- 名称：登录后进入首页展示 KPI 三区与图表区域
- 功能集合：页面加载
- 步骤：进入 /Apex/dashboard → 观察 KPI 三区与图表区

**样例 2 — 图表展示**

- 名称：资源使用情况卡片呈置灰不可操作状态
- 功能集合：图表展示
- 预期：卡片置灰、项目下拉不可点、图表区域仍可见

## 复跑命令

```bash
cd .cursor/test-skills/输出csv的测试用例
python scripts/generate_feature_csv.py \
  --cases configs/dashboard.cases.json \
  --template ../../../docs/问题单/模板/dashboard.csv \
  --output ../../../docs/问题单/0610/dashboard.csv \
  --force
```
