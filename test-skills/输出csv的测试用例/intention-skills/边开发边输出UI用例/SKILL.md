---
name: 边开发边输出UI用例
description: "[已退役] v1 追加路径。请改用 intention-skills/基于源码+口述生成（路径 C）。"
---

# 边开发边输出 UI 用例（已退役）

> **v1 追加路径已无人使用。** 请路由至 `[[../基于源码+口述生成/SKILL.md]]`（路径 C）+ `generate_feature_csv.py`。

历史脚本 `append_ui_cases_to_csv.py` 见 `scripts/archive/`（若存在）或 `README.md` §Legacy，**禁止**作为默认路径。
