# 基于 test.ts 生成测试用例

扫描 Vitest test.ts 撰写 cases.json 并生成 CSV。API/gateway 路径自动路由。

## 用法

「整理 apex_dev 菜单 gateway/api 的 test.ts，模块名菜单管理，创建人员惠岩」
→ SKILL.md 路由至此 → 扫描 test.ts → 路由 api/gateway feature → 质量自检 → generate_test_csv.py → CSV
