# 同步运行记录 · 2026-07-21

## YAML 输出

```yaml
route: "编排-同步skill收益"
sourceHarnessPath: "F:\\Documents\\Repertory\\Sieyuan\\nebula"
sourceDiffSummary: |
  强化 docs/QUALITY_LOOP.md（证据门：L0/L1/L2、Proof-or-stop、对抗审查、停条件、失败入库）；
  HARNESS_REVIEW 五模块挂质量 Loop + 心智写入流程；ARCHITECTURE 主域/排查分层。
portableCapabilities:
  - { tier: P1, name: "质量 Loop（外证 DONE）", action: "合并增强验收问句" }
  - { tier: P1, name: "证据阶梯分层", action: "新增行" }
  - { tier: P1, name: "对抗审查与停条件", action: "新增行" }
  - { tier: P1, name: "失败入库路径", action: "新增行" }
  - { tier: P1, name: "人类审查导览", action: "合并：挂 Loop + 心智写入" }
rejectedAsBusinessSpecial:
  - Cookie-Session / Bearer 会话模型
  - Apex ≠ npm nebula 命名
  - seccenter 默认不改 Go
  - apex_dev/hytests、openCLI、run-*-matrix.mjs 等样例 L2 路径
  - N→1 租户删扇出等产品级 L2 案例
  - surface= 具体子仓名表
  - 把 QUALITY_LOOP 全文拷进 skill 主文件
skillFilesToPatch:
  - references/可迁移能力.md
  - references/样例-Nebula.md（槽位措辞）
  - evals/sync-run-2026-07-21.md（本记录）
darwinFollowUp: "evaluate-only"
stopOrCheckpoint: "人已要求真实跑同步；写盘仅限 skill 套件 references，未改业务仓"
sampleLeakScan: "通过"
```

## Darwin evaluate-only（干跑）

- 父 SKILL 未改；能力表增长 3 行（证据阶梯/对抗审查/失败入库）+ 2 行合并增强  
- 无业务特例泄漏进能力名列  
- 建议：他仓下次「升级 harness」对照新增 P1 三行勾缺口  

## 未改

- 父 `SKILL.md`、业务仓 `nebula/**` 源码与 docs（只读）  
