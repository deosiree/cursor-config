# 执行检查清单

## 共用

- [ ] 已解析 `repo`、`shared_branch`
- [ ] 已跑分析 → 策略得唯一 `phase`
- [ ] 无 force / 无 `reset --hard`
- [ ] 未误路由邻近 skill

## push_draft

- [ ] CHECKPOINT-DRAFT-PUSH；记下 `draftSha`

## local_review

- [ ] 临时分支 mixed reset；改动在工作区

## hotfix_during_review

- [ ] CHECKPOINT-HOTFIX-STASH（告知默认不 pop）
- [ ] develop 对齐 origin[+upstream]
- [ ] CHECKPOINT-HOTFIX-PUSH；未 amend 草稿
- [ ] 双远端 push 结果已报告；记下 `hotfixSha`
- [ ] CHECKPOINT-RECLAIM-SERIES：`draft^..tip` 列表已确认
- [ ] `checkout -B temp tip` + `reset draft^`；工作区为整包
- [ ] 旧 hotfix stash 已 drop（除非用户要保留仅本地改动）
- [ ] `draftSha` 未改写

## publish_final

- [ ] revert **系列**（新→旧，含热修）
- [ ] final 含仍需要的热修内容
- [ ] cherry-pick final + push；删临时分支

## 输出字段

`phase` / `draftSha` / `hotfixSha` / `seriesTip` / `reviewBase` / `tempBranch` / `finalSha` / `status`
