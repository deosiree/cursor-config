# 语料 Goal 收工门禁（SSOT）

> 版本：`v1.1.0`  
> 核验：`python scripts/check-rag-corpus-gates.py`  
> `GOALS.md` 只展示脚本摘要，**人工勾选不算 PASS**。  
> 改阈值须人类确认后升版本号。

## 诚实前提

仅有单模块 FAQ/SOP **不能**宣称测集就绪。本版本起：旅程未齐或 styles 去垫 md 数不足 → **FAIL**。

## 三类库就绪

| 库 | 硬阈值 |
|----|--------|
| Corpus | 去垫汉字 ≥ **80000**；styles 去垫 `.md` ≥ **200**；覆盖矩阵与旅程矩阵全绿 |
| Runtime | `split=runtime` ≥ **40**；与 test 题面不重叠 |
| Test | `split=test` ≥ **80**；可溯源 |

金标合计 ≥ **120**；每 `journey_id` ≥ **6** 条（tags 含 journey）。

## L1 结构

- 去垫统计；单文件汉字 ≤ **12000**
- 模块矩阵：FAQ / 排障 / 场景 / 说明书 / SOP（按产品裁剪）
- 跨模块旅程见 `eval/journeys-matrix.yaml`
  - 单篇旅程汉字 ≥ **2000**
  - 须含章节锚点：`前置`、`步骤`、`模块切换`、`失败`、`证据`
  - 文中点名 ≥ 3 个模块（侧栏名或 id）

## L3 黑名单（摘要）

- 只写单模块、无切换与交接  
- 附录/加厚凑篇数  
- 旅程步骤与源码按钮名不一致  
- 覆盖人类截图  

## 截图

人截优先；文档 Goal 可 `skip_shots=true`。

## 人类确认点

1. 本文件版本变更  
2. 首次在本版本下脚本全绿时的收工声明
