# 语料 Goal 收工门禁（摘录 · 实战 v1.1.0）

> 核验：`python scripts/check-rag-corpus-gates.py`  
> `GOALS.md` 只展示脚本摘要，**人工勾选不算 PASS**。

## 三类库就绪（摘录）

| 库 | 硬阈值 |
|----|--------|
| Corpus | 去垫汉字 ≥ **80000**；styles 去垫 `.md` ≥ **200**；旅程矩阵全绿 |
| Runtime | `split=runtime` ≥ **40** |
| Test | `split=test` ≥ **80** |

### 跨模块旅程

- 单篇旅程汉字 ≥ **2000**
- 章节锚点：`前置`、`步骤`、`模块切换`、`失败`、`证据`
- 每旅程 ID 至少 **6** 条金标

## L3 黑名单（摘录）

- 只写单模块 SOP、不写跨模块切换与数据交接  
- 用「加厚/附录」凑过 200 篇  
- 旅程步骤与 Vue 按钮名不一致
