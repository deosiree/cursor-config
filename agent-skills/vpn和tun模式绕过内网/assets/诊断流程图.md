# VPN TUN 模式内网访问诊断流程图

## 完整决策树

```mermaid
graph TD
    Start([用户报告: 开 TUN 后无法访问内网]) --> CheckAccess{能否 ping 通内网 IP?}
    
    CheckAccess -->|能 ping 通| DNS_Issue[问题在应用层]
    CheckAccess -->|ping 不通| Route_Issue[问题在网络层]
    
    %% ============ 应用层分支 ============
    DNS_Issue --> CheckDNS{nslookup 内网域名<br/>返回 198.18.x.x?}
    CheckDNS -->|是| Solution_FakeIP[方案: fake-ip-filter]
    CheckDNS -->|否| CheckApp{特定应用无法访问?}
    CheckApp -->|是| Solution_Proxy[方案: 检查应用代理设置]
    CheckApp -->|否| Solution_Firewall[方案: 检查防火墙规则]
    
    %% ============ 网络层分支 ============
    Route_Issue --> CheckTUN{检查 TUN 接口 metric}
    CheckTUN --> TUNMetric{TUN metric < 物理网卡 metric?}
    
    TUNMetric -->|是<br/>TUN 劫持了流量| CheckRouteTable{检查路由表}
    TUNMetric -->|否| Solution_MetricAdjust[方案: 调整 metric<br/>或检查其他问题]
    
    CheckRouteTable --> HasInternalRoute{内网路由存在?}
    HasInternalRoute -->|存在| CheckRouteMetric{内网路由 metric<br/>< TUN metric?}
    HasInternalRoute -->|不存在| Solution_StaticRoute[方案 C: 添加静态路由]
    
    CheckRouteMetric -->|是<br/>优先级正确| Solution_Other[方案: 检查其他因素<br/>网关/子网掩码/DNS]
    CheckRouteMetric -->|否<br/>被 TUN 覆盖| Solution_TUNExclude[方案 A: TUN 路由排除<br/>推荐]
    
    %% ============ 解决方案详情 ============
    Solution_TUNExclude --> Detail_A["配置 inet4-route-exclude-address:<br/>- 10.0.0.0/8<br/>- 172.16.0.0/12<br/>- 192.168.0.0/16"]
    
    Solution_FakeIP --> Detail_B["配置 fake-ip-filter:<br/>添加内网域名通配符<br/>如 *.corp, *.internal"]
    
    Solution_StaticRoute --> Detail_C["Windows: route add<br/>Linux: ip route add<br/>macOS: route -n add"]
    
    Detail_A --> Verify[验证: ping/curl 内网地址]
    Detail_B --> Verify
    Detail_C --> Verify
    Solution_Proxy --> Verify
    Solution_Firewall --> Verify
    Solution_MetricAdjust --> Verify
    Solution_Other --> Verify
    
    Verify --> Success{访问成功?}
    Success -->|是| End_Success([问题解决])
    Success -->|否| Escalate[升级诊断:<br/>1. 抓包分析<br/>2. 检查 VPN 日志<br/>3. 联系网络管理员]
    
    Escalate --> End_Escalate([需进一步排查])
    
    style Start fill:#e1f5ff
    style End_Success fill:#c8e6c9
    style End_Escalate fill:#ffccbc
    style Solution_TUNExclude fill:#fff9c4
    style Solution_FakeIP fill:#f0f4c3
    style Solution_StaticRoute fill:#ffe0b2
```

## 快速判断表

| 症状 | 可能原因 | 优先方案 |
|------|----------|----------|
| ping 不通内网 IP | TUN 劫持路由 | TUN 路由排除 (方案 A) |
| ping 通但浏览器无法访问 | DNS 返回 fake-ip | fake-ip-filter (方案 B) |
| 部分网段可访问，部分不可 | 路由表缺失 | 静态路由 (方案 C) |
| 关闭 TUN 就正常 | TUN metric 过低 | 方案 A 或调整 metric |
| 特定应用无法访问 | 应用代理配置 | 检查应用设置 |

## 诊断命令速查

### Windows
```powershell
# 查看路由表
route print

# 查看网络接口 metric
Get-NetIPInterface | Select-Object InterfaceAlias, InterfaceMetric | Sort-Object InterfaceMetric

# 测试连通性
ping 10.17.196.39
Test-NetConnection -ComputerName 10.17.196.39 -Port 80

# DNS 解析测试
nslookup internal.corp
```

### Linux/macOS
```bash
# 查看路由表
ip route show        # Linux
netstat -rn          # macOS

# 查看 TUN 接口
ip link show         # Linux
ifconfig             # macOS

# 测试连通性
ping -c 4 10.17.196.39
curl -v http://10.17.196.39

# DNS 解析测试
nslookup internal.corp
dig internal.corp
```

## 常见陷阱

1. **只配置 fake-ip-filter**：仅解决域名问题，IP 直连仍会失败
2. **静态路由 metric 过高**：被 TUN 路由覆盖，需设置为 < TUN metric
3. **重启后路由失效**：静态路由不持久化，需添加启动脚本
4. **子网掩码错误**：导致路由匹配失败，需精确配置
5. **网关地址错误**：使用 VPN 网关而非物理网关，流量仍被劫持

## 下一步

- 详细错误模式 → [常见错误模式](常见错误模式.md)
- TUN 工作原理 → [Clash TUN 原理](../references/clash-tun-原理.md)
- 真实案例参考 → [真实案例库](../references/真实案例库.md)
