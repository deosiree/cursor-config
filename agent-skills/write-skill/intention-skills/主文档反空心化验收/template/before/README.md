# before：空心问题态

这是当前 intention 唯一允许保留的空心 `before`。

## 允许原因
- 这个节点本身就是为了识别“主文档 / 模板空心化”而存在。
- 因此它的 `before` 可以直接展示“只有门禁说明，没有足够模板证据”的问题态。

## 当前缺口
- `template/after` 还没有真实 sample node
- 模板层只有说明文档，缺少成品态快照
- callback 无法仅靠模板直接确认它已经通过自反查
