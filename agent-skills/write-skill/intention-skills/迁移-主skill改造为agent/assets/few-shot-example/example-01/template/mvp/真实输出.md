# 真实输出

```json
{
  "selectedPath": ["架构拓扑映射", "源码符号定位", "文档结构规划", "源码证据抽取", "图类型判定", "文档内容生成", "Mermaid图语法修复", "README索引维护"],
  "parallelGroups": [["源码证据抽取", "图类型判定"]],
  "gates": ["认知充分", "结构已收敛", "图语法通过", "索引可导航"],
  "rollbackMap": {
    "拓扑不完整": "架构拓扑映射",
    "符号定位缺失": "源码符号定位",
    "图语法失败": "Mermaid图语法修复"
  },
  "humanQuestions": [],
  "nextIterationAction": "先生成拓扑与结构规划，再进入正文写作"
}
```

审计说明：

- 选路原因：目标是“全量多文件文档体系”，不是单文档修补
- 并行理由：`源码证据抽取` 与 `图类型判定` 互不阻塞，可并行完成
- 回退原则：一旦图语法失败，不回退到结构规划，优先局部修图
