# Darwin 最终报告 · hermes-session-harvest

> 评估日期：2026-06-02
> 总轮次：4（Baseline → R1 → R2 → R3 → R4）
> 最终分数：**65.7 / 100**

## 得分演变

```
55.3 ──R1(+3.0)──▶ 58.3 ──R2(+1.4)──▶ 59.7 ──R3(+1.0)──▶ 60.7 ──R4(+5.0)──▶ 65.7
 基线     边界条件      检查点       资源整合      实测(dry)
```

## HL 层级达成

| 层级 | 描述 | 状态 | 贡献 |
|:---:|------|:---:|------|
| HL-1 | 结构存在性 (SKILL+feat+evals+refs) | ✅ | +8~9 (基线已有) |
| HL-2 | 内容充实度 (template/test-prompts 真实样本) | ✅ | +2~3 (R3完成) |
| HL-3 | 跨节点一致性 (引用可解析、无死链) | ✅ | +1~2 (天然满足) |
| HL-4 | 执行验证 (dry_run通过) | ⚠️ 部分 | +5 (R4完成) |

## 边际收益曲线

```
轮次    Δ     累积    边际变化
R1     +3.0   58.3    ████████
R2     +1.4   59.7    ████          ← 开始衰减
R3     +1.0   60.7    ███           ← 继续衰减
R4     +5.0   65.7    ████████████  ← 权重25维度，单次大跳
```

R1→R2→R3 呈现典型的衰减模式（3.0→1.4→1.0），R4 的 +5.0 是因为维度 8 权重极高（25），dry_run 从 3→5 拉了一把。但这是"一次性红利"——后续提升需要真实会话。

## HL-4 拐点判定

| 指标 | 阈值 | 当前 | 判定 |
|------|:---:|:---:|:---:|
| 本轮改进幅度 <5% | <5% | 8.3% (R4) | 未触发 |
| 下一轮预计改进 | — | +1.5~3.0 | 边际低 |
| 剩余提分空间 | — | 19.3 分 | 需 HL-4 真实执行 |
| 单点成本 | — | 高（需要真实OpenCLI会话） | 投入产出比拐点 |

**判定：已达到 HL-4 拐点，但未到 <5% 硬着陆。**

## 建议：收手

理由：
1. **82% 的剩余分数卡在真实会话验证上**——前端维度（frontmatter/架构/指令具体性）最多再提 +6~8 分，做不到 85
2. **维度 8（25分权重）想要再提，必须真实跑一次**——单个 dry_run 已到上限
3. R2/R3 已出现 <3% 的边际收益衰减模式，说明"纸面优化"空间即将枯竭
4. 类比 `输出csv的测试用例` 案例：边际收益从 +8.7 降至 +2.5 时收手；当前 R3 已到 +1.0

### 最值得优化的一点

> **在下次真实的 OpenCLI 修复会话中，让 Agent 走完整触发链路并记录实测反馈。**

这是唯一能推动维度 8 从 5 到 7-8 的路径（价值 +5~7.5 分）。任何纸面优化都无法替代这次真实会话。

### 如果坚持继续优化

按优先级排：
1. 整体架构 6→7 (+1.5) — 主 SKILL.md 从 315 行瘦身到 ~220 行，下沉部分内容到 references
2. 指令具体性 7→8 (+1.5) — 给每个 SCAN 步骤加上明确的工具调用名（`search_content`/`read_file`/`glob`）
3. Frontmatter 6→7 (+0.8) — 补 `version`/`author` 字段

但这三轮总计只能提 ~3.8 分，把总分推到 ~69.5——仍然不到 85。**真正让分冲破 80 的唯一路径是真实会话验证。**
