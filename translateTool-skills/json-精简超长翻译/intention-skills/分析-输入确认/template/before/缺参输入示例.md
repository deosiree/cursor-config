# 缺参输入示例（失败态）

## 场景：用户只给了 inputPath，没给 byteLimit

```text
使用 $json-精简超长翻译
  --inputPath F:\path\to\db
```

## 问题

`byteLimit` 缺失，无法确定裁剪标准。

## 应输出

```json
{
  "action": "abort",
  "missingFacts": ["byteLimit"],
  "message": "请提供 UTF-8 字节上限（如 --byteLimit 63）"
}
```
