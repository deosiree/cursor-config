# 编排-精简工作流

## 作用

根据检测报告判断：有超标词条 → 路由到 feature 缩短流程；全部合规 → 直接复制输出。编排"缩短→回验→写输出"的迭代顺序。

## 资源入口

- 模板：`[[template/before/]]` 有超标的场景
- 模板：`[[template/after/]]` 全合规无需缩短的场景
- 校验：`[[evals/evals.json]]`

## 使用示例

```text
检测报告显示有 5 条词条超长。
使用 $编排-精简工作流 路由到缩短流程。
```
