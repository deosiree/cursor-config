# en2ru 质量环实跑记录（2026-07-15）

## 输入
- `去重文件（去重后，送翻前）_词条导出_20260714075255_RU机翻.xlsx`
- 备份：`*_RU机翻_preQualityLoop.xlsx`

## 引擎
- 硬门禁：`validateEn2RuQuality`（CJK / 换行对齐 / 括注 / 错位启发）+ 收紧 `isAllowedEnRuIdentity`
- 质量环：`scripts/remediate-en2ru-until-clean.js`
- 验收模型：`deepseek:deepseek-v4-flash`

## 结果
- 首轮硬失败约 39 条（CJK 7 / echo 20 / nl 1 / misalign 6）
- 迭代清污后：`pass=true`，`empty=0`，`filled=3842`，`recommendDeliver=true`
- 产物：同目录 `en2ru-quality-loop.json`（`verifyModel: deepseek:deepseek-v4-flash`）
- 额外硬门禁：拉丁扩展乱码（如越南语 ẫ）、收紧 identity 白名单、`F%1` 格式标记可同形

## 抽检结论（相对用户指出的问题）
- 与英源换行数一致的多行串（如 `%2` 前换行）**不记缺陷**
- `vcenter` / Caps Lock / Envelope / Didot / Val 等不再白名单放行，已送翻
- 中俄混搭行已清除汉字
- `Black` 错位类由 misalign 启发捕获并重译

## 命令复现

```bash
node scripts/test-en2ru-residual.js
node scripts/remediate-en2ru-until-clean.js --in "<RU机翻.xlsx>" --max-rounds 5 --batch-size 10
node scripts/verify-post-translate.js --out "<RU机翻.xlsx>"
```
