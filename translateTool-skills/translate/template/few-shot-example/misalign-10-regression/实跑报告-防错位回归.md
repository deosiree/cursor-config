# 实跑报告 · misalign-10 防错位回归

- **时间**：2026-07-15
- **引擎**：换行哨兵 `⟦__NL__⟧` + 条数门禁 + 拒写 + DeepSeek-R1 批次成功（讯飞 Busy → Hunyuan 条数 0 拒收 → failover）
- **验收 skill**：`feature-skills/验证-译后防错位`
- **acceptanceReport**：`pass=true`，`stillLooksMisaligned=[]`，`recommendDeliver=true`

## 对照（旧错位 vs 新译文）

| id | 旧（串行） | 新（应对齐原文） |
|----|------------|------------------|
| …QButtonGroup… | Flags properties… | 关于 QButtonGroup `%1`/`%2` |
| read-only property | Запуск (Launch) | 只读属性 `%1` |
| Designer UI files | 非均匀缩放… | `*.%1` 文件过滤器 |
| Attribute group | Bad HTTP request | 属性组 `%1` 已定义 |
| audio device html | Параметр Default | html 音频设备 `%1` |
| property/type write | 音频设备 html 片段 | 属性 `%1` 类型 `%2` |
| color map depth | Функция Henkan | 色深 `(%1)` |
| Operator | Detailed View | 运算符 `%1` 类型 `%2` |
| Value exceeds | Editable | 值 `%1` 类型 `%2` 最大 `%3` |
| Connecting…\\n%1 | 缺 `%1` | 两行 + `%1` |

## Darwin 记录（evaluate）

| 项 | 值 |
|----|-----|
| skill | 验证-译后防错位（新建）+ translate 路由补强 |
| eval_mode | full_test |
| status | keep |
| note | 10/10 毒行重跑通过；条数不齐拒收生效 |

## 命令复现

见同目录 [README.md](./README.md)。
