# After — 全流程通过（OOM 排障）

## 预检
- morbax 已打开目标集群
- kubectl 在 jump 上可用

## 排查链

```text
# Step 1: get pods
> kubectl get pods -n platform -o wide | grep seccenter
  seccenter-v2-xxxxx   0/1   CrashLoopBackOff   14   2h

# Step 2: describe
> kubectl describe pod seccenter-v2-xxxxx -n platform | tail -30
Events:
  Warning  OOMKilling   Memory cgroup out of memory
Last State: Terminated Reason:OOMKilled Exit Code:137

# Step 3: top
> kubectl top pod -n platform --sort-by=memory | tail -5
  seccenter-v2-xxxxx   950Mi   1024Mi   (93% of 1Gi limit)
```

## 输出交付

```
pod: seccenter-v2-xxxxx
podStatus: CrashLoopBackOff
restartCount: 14
lastState: OOMKilled (Exit Code: 137)
events: OOMKilling → Memory cgroup out of memory
fixHint: 内存 93%，增大 resources.limits.memory 到 2Gi 或排查泄漏
```
