# After — 全流程通过

## 预检

- morbax 已打开目标集群（48 / cloudtest / 47）
- `config/ssh.config.local.json` 已配置凭据
- plink/ssh 可用，hostkey 已缓存

## SSH 查日志

```powershell
kubectl get pods -n platform | grep seccenter
> seccenter-v2-6d8bb9f9c-rfmzz   1/1     Running
> seccenter-v2-6d8bb9f9c-xqkln   1/1     Running

kubectl logs --tail=10000 seccenter-v2-6d8bb9f9c-rfmzz -n platform 2>&1 | grep -E 'ERRO|ImportProjectMenuTree'
> ERRO server端系统seccenter-v2异常: 菜单 '查看首页' 的 ID 无效: 0
```

## 输出交付

```
pod: seccenter-v2-6d8bb9f9c-rfmzz
erroLine: ERRO server端系统seccenter-v2异常: 菜单 '查看首页' 的 ID 无效: 0
stackHint: validateProjectImportMenus → ImportProjectMenuTree
frontendToast: [100000]未知错误
fixHint: patch_children_add 补真实 id → ensure-patch-menu-ids.py → merge → dry_run 再验证
```

## 验证结果

- HTTP dry_run 返回 `PREVIEW OK: deleted=N created=N`（N>0）
- 或以 `exit code 0` 成功完成
