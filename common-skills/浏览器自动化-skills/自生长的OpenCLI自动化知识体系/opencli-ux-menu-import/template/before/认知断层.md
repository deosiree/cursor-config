# 执行前认知

用户说「帮我把这个 YAML 导入到系统」时，Agent 容易：

1. 只跑 HTTP dry_run，不查 SSH ERRO → toast `[100000]` 误导
2. 发现 id=0 但不知道 `ensure-patch-menu-ids.py` 存在
3. 不先 merge 就预览 → PyYAML 结构顺序错误
4. 用错 API base（8080 vs 8082）→ 404
