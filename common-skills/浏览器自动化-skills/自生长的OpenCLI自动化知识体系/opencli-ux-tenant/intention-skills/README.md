# 意图层 — 判断执行场景

## 定位

意图层只做一件事：根据用户需求和环境状态，**判断该跑什么**。

## 子节点

| 节点 | 职责 |
|------|------|
| `判断执行场景/` | 选择 profile、流程范围（全量/部分）、是否跳过登录 |

## 边界

- 不执行测试（交给 feature-skills）
- 不诊断失败（交给 `诊断失败原因`）
- 不修改配置（交给用户手动改或 references 指引）
