# After — 全流程通过（会话目标态）

## 预检

- `opencli doctor` OK
- user0601 已登录 / bind
- session 含 `sys:user:add`（UI 新增可选）

## 创建种子用户

```powershell
$js = Get-Content -Raw '.cursor/nebula-skills/opencli-ux-user-perm/scripts/create-seed-users.js'
opencli browser user0601 eval $js
```

输出：`created[6]` + `rows` 中他人行含多项 ops 或 hasMore。

## 权限诊断

- 单测 `hasPerm.test.ts` 通过
- 浏览器 eval：本人行 ops 少、u0601_* 行 ops 多或 hasMore=true

## 清理（可选）

```powershell
$js = Get-Content -Raw '.cursor/nebula-skills/opencli-ux-user-perm/scripts/cleanup-users-to-n.js'
opencli browser user0601 eval $js
```

`afterCount: 10`，保留 user0601 + owner + 最新种子用户。

## 知识沉淀

- skill 路径：`.cursor/nebula-skills/opencli-ux-user-perm/`
- Darwin Round1：GOOD (67/80)
