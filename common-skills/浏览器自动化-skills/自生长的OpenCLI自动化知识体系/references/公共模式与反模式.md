# 公共模式与反模式

> 从 `opencli-ux-role-tab-validation` 和 `opencli-ux-tenant` 的 `lib/common.sh` 中提取的可复用模式。适用于自动化测试、手动操作、爬虫三大场景。

---

## 模式

### P1: eval JSON 断言（最通用）

```bash
# 用 eval 返回结构化 JSON，Python 提取字段做断言
result=$(opencli browser $SESSION eval "
  JSON.stringify({
    exists: !!document.querySelector('.el-dialog'),
    activeTab: document.querySelector('.el-tabs__item.is-active')?.innerText?.trim(),
    toast: document.querySelector('.el-message')?.innerText?.trim(),
  });
")
echo "$result" | python3 -c "
import sys, json
d = json.load(sys.stdin)
assert d.get('exists'), '弹窗未出现'
print('PASS: 弹窗存在, activeTab=' + d.get('activeTab', ''))
"
```

**来源：** `assert_role_dialog_tab`, `assert_role_form_error`（lib/common.sh）
**优势：** 一次 eval 获取多个断言点，减少浏览器交互次数

### P2: click → eval 兜底

```bash
# 先用 click --name，失败后用 eval JS 兜底
opencli browser $SESSION click --role button --name "确定" >/dev/null 2>&1 || {
  opencli browser $SESSION eval "
    (function(){
      const label = '确定';
      const footer = document.querySelector('.dialog-footer')
        || document.querySelector('.el-dialog__footer');
      const btn = [...footer.querySelectorAll('button')].find(
        b => (b.innerText || '').replace(/\\s+/g, '').trim() === label
      );
      if (btn) { btn.click(); return JSON.stringify({ ok: true }); }
      return JSON.stringify({ ok: false });
    })()
  " >/dev/null 2>&1
}
```

**来源：** `click_dialog_footer_button`（lib/common.sh）
**优势：** 兼容 Element Plus / Ant Design 等组件库的按钮渲染差异

### P3: fill → eval 兜底（触发框架事件）

```bash
# fill 不触发 Vue/React 的 input 事件时，用 eval 设置值
opencli browser $SESSION fill --role textbox --name "角色名称" "测试" >/dev/null 2>&1 || {
  opencli browser $SESSION eval "
    (function(){
      const value = '测试';
      const items = [...document.querySelectorAll('.el-form-item')];
      for (const item of items) {
        const label = item.querySelector('.el-form-item__label');
        if (!label || !label.innerText.includes('角色名称')) continue;
        const input = item.querySelector('input:not([type=hidden])');
        if (input) {
          const desc = Object.getOwnPropertyDescriptor(
            window.HTMLInputElement.prototype, 'value'
          );
          if (desc && desc.set) desc.set.call(input, value);
          else input.value = value;
          input.dispatchEvent(new Event('input', { bubbles: true }));
          input.dispatchEvent(new Event('change', { bubbles: true }));
          return JSON.stringify({ ok: true });
        }
      }
      return JSON.stringify({ ok: false });
    })()
  " >/dev/null 2>&1
}
```

**来源：** `fill_role_name`（lib/common.sh）
**优势：** 解决 `fill --role textbox` 对自定义输入框的无效填充

### P4: JSON 配置 + 本地覆盖

```bash
# 仓库内 config.json 含默认值，local.json（gitignore）覆盖敏感字段
# 用 Python 或 jq 合并两个 JSON

# 方法一：jq
merged=$(jq -s 'reduce .[] as $item ({}; . * $item)' config/base.json config/local.json)

# 方法二：Python
merged=$(python3 -c "
import json, sys
with open(sys.argv[1]) as f: a = json.load(f)
with open(sys.argv[2]) as f: b = json.load(f)
def merge(x, y):
    out = dict(x)
    for k, v in y.items():
        if k in out and isinstance(out[k], dict) and isinstance(v, dict):
            out[k] = merge(out[k], v)
        else:
            out[k] = v
    return out
print(json.dumps(merge(a, b), ensure_ascii=False))
" config/base.json config/local.json)
```

**来源：** `_merge_json_files`（lib/config.sh）
**优势：** 密码、token 等敏感信息不提交 git，本地覆盖即可

### P5: 验证码处理策略（三种模式）

| 模式 | 适用场景 | 行为 |
|------|---------|------|
| `auto` | 开发环境无验证码 | 直接登录，有验证码则报错 |
| `manual` | 有图形验证码 | 暂停等待人工输入后继续 |
| `bind-only` | 复杂 MFA 验证 | 不自动登录，人工登录后 `opencli browser <session> bind` |

**来源：** `handle_captcha_mode`（lib/common.sh）

### P6: 等待跳离登录页（超时重试）

```bash
# 登录后等待 URL 离开 /login 路径
wait_leave_login() {
  local session="$1"
  for _ in $(seq 1 60); do
    local url
    url="$(opencli browser $session get url | tr -d '\r\n')"
    if [[ "$url" != *"/login"* ]]; then
      echo "已离开登录页: $url"
      return 0
    fi
    sleep 1
  done
  echo "登录超时（60s 内未离开 /login）" >&2
  return 1
}
```

**来源：** `wait_leave_login`（lib/common.sh）

### P6: Element Plus append-to-body 弹窗（eval + 可见 overlay）

`state` 里 dialog 常为空壳；应对 `display:block` 的 `.el-overlay` 读写表单。

```bash
# 打开弹窗
opencli browser $SESSION eval "[...document.querySelectorAll('button')].find(b=>b.innerText.trim()==='新增')?.click()"
opencli browser $SESSION wait text "路由路径" --timeout 15000

# 读错误
opencli browser $SESSION eval "
  (() => {
    const o = [...document.querySelectorAll('.el-overlay')].find(x => getComputedStyle(x).display === 'block');
    return JSON.stringify([...o.querySelectorAll('.el-form-item__error')].map(e => e.textContent.trim()));
  })()
"
```

**来源：** `opencli-ux-menu/references/element-plus-overlay-pattern.md`、`lib/common.sh#get_menu_form_state`

### P7: sessionStorage 权限上下文诊断

```bash
opencli browser $SESSION eval "(function(){
  const u=JSON.parse(sessionStorage.getItem('userInfo')||'{}');
  const perms=u.permissions||u.perms||[];
  const bypass=u.isOwner===true;
  function check(p){
    if(bypass) return true;
    if(!perms.includes(p)) return false;
    return !!u.permsMap?.[p];
  }
  return JSON.stringify({
    isOwnerStrict: bypass,
    tests: {
      profile: check('sys:profile:view'),
      dashboard: check('sys:dashboard:view')
    }
  });
})()"
```

**来源：** `session-log/2026-06-02-perm-bypass-isOwner-header.md`、`references/场景-权限与登录态诊断.md`
**用途：** 区分「bypass 失效」与「角色缺 perm」；不依赖 Pinia store

### P8: 登录后 Header 断言（禁止 reload）

```bash
# 登录完成后直接测，不要用 location.reload()
opencli browser $SESSION click <用户下拉ref>
opencli browser $SESSION eval "(function(){
  const items=[...document.querySelectorAll('[role=menuitem]')].map(el=>(el.textContent||'').trim());
  return JSON.stringify({ profileVisible: items.includes('个人中心') });
})()"
```

**来源：** NavbarActions `canViewProfile` computed 缓存问题验收
**要点：** reload 会掩盖「登录后 computed 未订阅 userInfo」类 bug

---

## 反模式

### A1: `sleep` 代替条件等待

```bash
# ❌ 反模式
sleep 5
opencli browser $SESSION click --role button --name "提交"

# ✅ 正确
opencli browser $SESSION wait text "提交" --timeout 10000
opencli browser $SESSION click --role button --name "提交"
```

**问题：** 网络波动时 sleep 不足或过长，脆弱且慢。

### A2: eval 返回自由文本

```bash
# ❌ 反模式
opencli browser $SESSION eval "document.title"

# ✅ 正确
opencli browser $SESSION eval "JSON.stringify({ title: document.title })"
```

**问题：** 自由文本含换行/空格，shell 变量捕获困难。JSON 结构化可精确提取。

### A3: 每个脚本新建 session

```bash
# ❌ 反模式
SCRIPT_A: opencli browser session-a open ...
SCRIPT_B: opencli browser session-b open ...

# ✅ 正确
ALL SCRIPTS: opencli browser nebula-ux open ...
```

**问题：** 每个 session 需重新登录 + 验证码，耗时且脆弱（见各子 skill 共用 `nebula-ux` 的实践）。

### A4: 硬编码敏感信息

```bash
# ❌ 反模式
PASSWORD="my_real_password_123"

# ✅ 正确
# config.json: { "password": "CHANGE_ME" }
# config.local.json（gitignore）: { "password": "my_real_password_123" }
```

**问题：** 不小心 git push 导致密码泄露。

### A5: 断言失败时不截图

```bash
# ❌ 反模式
# 只输出错误信息，无视觉证据

# ✅ 正确
screenshot() {
  opencli browser $SESSION screenshot "screenshots/fail-${case}.png"
}
echo "断言失败" >&2
screenshot "tc1"
exit 1
```

**问题：** 只看文字错误无法判断页面实际状态（是否渲染异常？定位偏了？）。

### A6: 登录后用 reload 验收 Header 权限 UI

```bash
# ❌ 反模式：login → location.reload() → 测下拉
# ✅ 正确：login → 等待跳转 → 直接 click 用户下拉 eval
```

**问题：** reload 重建组件，computed 重算，无法复现「登录后 UI 不刷新」。

### A7: browser 子命令误用 adapter 全局 flag

```bash
# ❌ opencli browser $SESSION open URL --format json
# ❌ opencli browser $SESSION network --limit 20
# ✅ open 直接执行；network 用 --detail "POST host/path"
```

**问题：** `-f/--format`、`--limit` 仅部分 adapter 命令支持，browser 子命令会报 `unknown option`。

### A8: 用 sessionStorage 判 perm 但不订阅 Vue userInfo

前端 `computed(() => checkHasPerm(...))` 若内部只读 sessionStorage，登录后 Pinia 已更新但 computed 可能缓存 false。

**OpenCLI 测法：** 登录后**不 reload**，用 P8 断言下拉。
**前端 fix：** 登录态下 `void userInfo.isOwner`（见 `perm-bypass-isOwner-pitfalls.md`）。

---

## eval 兜底准则

当 `click --name` / `fill --role textbox` 等结构化命令定位不到元素时，统一使用 eval JS 兜底：

```
1. 先用结构化命令（95% 情况够用）
2. 失败时降级到 eval JS
3. eval 内部按以下优先级定位：
   a. CSS 选择器：document.querySelector()
   b. 文本内容过滤：[...els].find(el => el.innerText.includes('关键词'))
   c. 角色/属性过滤：[...els].filter(el => el.getAttribute('role') === 'button')
4. 始终返回 JSON.stringify({ ok: boolean, ...data })
```

这种模式来自子 skill 的 lib 库，在 Element Plus / Ant Design 等组件库环境下验证了数百次执行。

---

### P9: `--profile` Chrome 用户级隔离（双会话 E2E）

```bash
# 两个不同的 Chrome 用户 → cookie 完全隔离
opencli --profile admin-profile browser perm-a open http://localhost:8080/cloud/login
opencli --profile test-profile browser perm-b open http://localhost:8080/cloud/login
```

**来源：** `gen-perms-apis/feature-skills/OpenCLI双会话权限验证/`（2026-06-06）
**前置：** `opencli doctor` 必须显示 ≥2 个 Profiles。只有 1 个→停下来反馈用户创建第二个 Chrome 用户。
**优势：** 不同 session 名只是不同 tab，`--profile` 才是 Chrome 用户级别的 cookie 隔离。同一终端交替发命令即可。

### P10: `check`/`uncheck` 命令操作复选框

```bash
# ✅ 用 check/uncheck 替代 eval + classList 检测
opencli browser $SESSION check --role checkbox --name "导入菜单"
opencli browser $SESSION uncheck --role checkbox --name "新增菜单"
```

**来源：** 菜单管理权限 E2E 验证（2026-06-06）
**优势：** `is-checked` class 在 Element Plus 中可能不在 `.el-checkbox` 上，`check`/`uncheck` 命令内部正确处理。`changed: true/false` 返回值可判断是否实际变更。

### P11: "清空"→勾选 模式（批量 checkbox 优化）

```bash
# 先清空，再只勾选目标——比遍历全部判断状态更快
opencli browser $SESSION eval "
  document.querySelectorAll('.function-list-actions button').forEach(function(b){
    if(b.innerText.trim()==='清空') b.click();
  });
"
opencli browser $SESSION check --role checkbox --name "导入菜单"
opencli browser $SESSION check --role checkbox --name "导出菜单"
```

**来源：** 菜单管理权限 E2E 验证（2026-06-06）
**优势：** 8 轮场景测试，每轮只需 1 次清空 + N 次 check，比逐个判断 `is-checked` 状态再决定 toggle 高效 3x+。

### P12: `find` → `click <ref>` 精确定位

```bash
# 先用 find 获取 ref，再用 ref 精确点击
opencli browser $SESSION find --role button --name "huiyan"
# → { ref: 9, ... }
opencli browser $SESSION click 9

# 弹出下拉后，find 菜单项
opencli browser $SESSION find --role menuitem --name "退出登录"
# → { ref: 56, ... }
opencli browser $SESSION click 56
```

**来源：** 双会话 E2E 中的退出登录操作（2026-06-06）
**优势：** `click --role menuitem --name "退出登录"` 在下拉未展开时匹配不到元素。`find` 返回精确 ref，`click <ref>` 不依赖语义匹配。

### P13: `permsMap` 在 `userInfo` 内部

```bash
# ❌ 错误：permsMap 不是独立 sessionStorage key
opencli browser $SESSION eval "sessionStorage.getItem('permsMap')"
# → 永远返回 null

# ✅ 正确：permsMap 在 userInfo 内部
opencli browser $SESSION eval "
  var u=JSON.parse(sessionStorage.getItem('userInfo')||'{}');
  JSON.stringify({permsMapKeys: u.permsMap?Object.keys(u.permsMap):null});
"
```

**来源：** 权限 E2E 验证排障（2026-06-06）
**要点：** `userInfo` 是 `useStorage` 自动同步到 sessionStorage 的完整对象，`permsMap` 是其子字段。

### P14: 自定义树组件用 `.node-label` 定位

```bash
# Element Plus el-tree 用 .el-tree-node__label
# 但自定义树组件可能用 .node-label
opencli browser $SESSION eval "
  var el=[...document.querySelectorAll('.node-label')]
    .find(function(e){return e.textContent.trim()==='菜单管理'});
  if(el) el.click();
"
```

**来源：** 角色编辑弹窗中的权限树（2026-06-06）
**优势：** 不同组件库的树节点 class 不同。先用 `eval` 扫描所有 `*` 找到匹配文本的 DOM 元素，确认其 class 后再写选择器。

### P15: Element Plus 表格滚动容器（P-ElementPlus-ScrollbarWrap）

```javascript
const wrap =
  dialog?.querySelector(".el-scrollbar__wrap") ||
  dialog?.querySelector(".el-table__body-wrapper");
const hasVerticalScroll = wrap.scrollHeight > wrap.clientHeight;
```

**来源：** `opencli-ux-api-whitelist`（API 白名单弹窗，2026-06-04）
**问题：** `max-height` 表格的滚动在 `.el-scrollbar__wrap`，只测 `body-wrapper` 会误判无纵向滚动。
**详述：** `opencli-ux-api-whitelist/references/element-plus-table-scroll.md`

---

## 反模式

### A9: 不同 session 名 ≈ cookie 隔离

```bash
# ❌ 反模式：不同 session 名，同一 Chrome profile → cookie 共享
opencli browser session-a open http://localhost:8080/cloud/login  # 用户 A
opencli browser session-b open http://localhost:8080/cloud/login  # 用户 A（cookie 泄漏！）

# ✅ 正确：不同 --profile，不同 Chrome 用户 → cookie 完全隔离
opencli --profile profile-a browser session-a open http://localhost:8080/cloud/login  # 用户 A
opencli --profile profile-b browser session-b open http://localhost:8080/cloud/login  # 用户 B
```

**来源：** 双会话 E2E 踩坑（2026-06-06）
**问题：** `opencli browser <session>` 的 `<session>` 只是 tab 标识，底层共享 Chrome 用户目录的 cookie。

### A10: `sessionStorage.getItem('permsMap')` 误查独立 key

```bash
# ❌ 反模式
opencli browser $SESSION eval "sessionStorage.getItem('permsMap')"

# ✅ 正确
opencli browser $SESSION eval "
  JSON.parse(sessionStorage.getItem('userInfo')).permsMap
"
```

**问题：** `permsMap` 不是独立 sessionStorage key，是 `userInfo` 的子字段。误查永远返回 null，导致误判为"权限未加载"。

### A11: eval + `classList.contains('is-checked')` 判断复选框状态

```bash
# ❌ 反模式：is-checked class 可能在父元素上，cb 不一定是 .el-checkbox
document.querySelectorAll('.el-checkbox').forEach(function(cb){
  if(cb.classList.contains('is-checked')) cb.click();
});

# ✅ 正确：用 opencli check/uncheck 命令
opencli browser $SESSION check --role checkbox --name "导入菜单"
opencli browser $SESSION uncheck --role checkbox --name "新增菜单"
```

**问题：** Element Plus 的 `is-checked` class 可能在 `.el-checkbox` 包裹层上，`querySelectorAll('.el-checkbox')` 返回的元素层级取决于 DOM 结构。`check`/`uncheck` 命令内部处理了各种情况。
