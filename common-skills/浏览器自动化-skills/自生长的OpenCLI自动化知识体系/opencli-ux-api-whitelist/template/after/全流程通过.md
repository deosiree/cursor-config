# GREEN：全流程通过样本

## 执行

```powershell
.\test-api-whitelist-table-scroll.ps1 -BindOnly
```

## 实测结果（p2ejw7ww · 8080）

| 步骤 | 结果 |
|------|------|
| bind | `http://localhost:8080/cloud/Apex/system/menu` |
| 真实插入 50 条 | `inserted: 50`, ~19s 串行 |
| 点击白名单 | `clicked: true` |
| 行数 | 55（原有 + 新插） |
| 纵向滚动 | PASS — `el-scrollbar__wrap` scrollHeight 2237 > clientHeight 368 |
| 横向滚动 | N/A — 弹窗宽度足够（766px） |

## eval 片段（oneline 版）

```json
{
  "ok": true,
  "rowCount": 55,
  "hasVerticalScroll": true,
  "hasHorizontalScroll": false
}
```

脚本：`scripts/opencli-whitelist-scroll-eval-oneline.js`。
