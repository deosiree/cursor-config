# RED：常见失败样本

## 1. 错误滚动容器（body-wrapper）

`scripts/opencli-whitelist-scroll-eval.js` 只测 `.el-table__body-wrapper`：

- `scrollHeight === clientHeight` → `hasVerticalScroll: false`（误判）
- 实测 55 行 + `max-height: 400` 时纵向滚动在 `.el-scrollbar__wrap`

## 2. 并发插种

```javascript
// 反模式：Promise.all(50 × fetch) → 挂死或限流 ~294s
```

应使用 `opencli-whitelist-seed-50-oneline.js` 串行 `next()`。

## 3. 错误 profile / 端口

- `opencli browser` 无 `--profile p2ejw7ww` → 默认 q5prwymq 未连接
- `http://localhost:8081/cloud/login` → 与基座 Cookie 不一致

## 4. 权限

未分配 `sys:menu:whitelist` → `click --testid sys-menu-whitelist-btn` → `semantic_not_found`。
